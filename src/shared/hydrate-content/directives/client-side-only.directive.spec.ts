import { ClientSideOnlyDirective } from './client-side-only.directive';

describe('ClientSideOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ClientSideOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
